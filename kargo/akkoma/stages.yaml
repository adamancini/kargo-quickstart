apiVersion: kargo.akuity.io/v1alpha1
kind: Stage
metadata:
  name: dev
  namespace: akkoma
  annotations:
    kargo.akuity.io/color: red
spec:
  requestedFreight:
    - origin:
        kind: Warehouse
        name: akkoma-charts
      sources:
        direct: true
  promotionTemplate:
    spec:
      vars:
        - name: gitRepo
          value: https://github.com/adamancini/kargo-quickstart
      steps:
        - uses: git-clone
          config:
            repoURL: ${{ vars.gitRepo }}
            checkout:
              - branch: main
                path: ./repo
        - uses: yaml-update
          config:
            path: ./repo/akuity/akkoma-appset.yaml
            updates:
              - key: spec.generators.0.list.elements.0.chart_version
                value: ${{ chartFrom("oci://ghcr.io/adamancini/charts/akkoma").Version }}
        - uses: git-commit
          config:
            path: ./repo
            message: >-
              chore: promote akkoma ${{ chartFrom("oci://ghcr.io/adamancini/charts/akkoma").Version }} to dev
        - uses: git-push
          config:
            path: ./repo
            targetBranch: main
        - uses: argocd-update
          config:
            apps:
              - name: akkoma-${{ ctx.stage }}
                namespace: argocd
                sources:
                  - repoURL: "ghcr.io/adamancini/charts"
                    chart: akkoma
                    desiredRevision: ${{ chartFrom("oci://ghcr.io/adamancini/charts/akkoma").Version }}
---
apiVersion: kargo.akuity.io/v1alpha1
kind: Stage
metadata:
  name: staging
  namespace: akkoma
  annotations:
    kargo.akuity.io/color: yellow
spec:
  requestedFreight:
    - origin:
        kind: Warehouse
        name: akkoma-charts
      sources:
        stages:
          - dev
  promotionTemplate:
    spec:
      vars:
        - name: gitRepo
          value: https://github.com/adamancini/kargo-quickstart
      steps:
        - uses: git-clone
          config:
            repoURL: ${{ vars.gitRepo }}
            checkout:
              - branch: main
                path: ./repo
        - uses: yaml-update
          config:
            path: ./repo/akuity/akkoma-appset.yaml
            updates:
              - key: spec.generators.0.list.elements.1.chart_version
                value: ${{ chartFrom("oci://ghcr.io/adamancini/charts/akkoma").Version }}
        - uses: git-commit
          config:
            path: ./repo
            message: >-
              chore: promote akkoma ${{ chartFrom("oci://ghcr.io/adamancini/charts/akkoma").Version }} to staging
        - uses: git-push
          config:
            path: ./repo
            targetBranch: main
        - uses: argocd-update
          config:
            apps:
              - name: akkoma-${{ ctx.stage }}
                namespace: argocd
                sources:
                  - repoURL: "ghcr.io/adamancini/charts"
                    chart: akkoma
                    desiredRevision: ${{ chartFrom("oci://ghcr.io/adamancini/charts/akkoma").Version }}
---
apiVersion: kargo.akuity.io/v1alpha1
kind: Stage
metadata:
  name: prod
  namespace: akkoma
  annotations:
    kargo.akuity.io/color: blue
spec:
  requestedFreight:
    - origin:
        kind: Warehouse
        name: akkoma-charts
      sources:
        stages:
          - staging
  promotionTemplate:
    spec:
      vars:
        - name: gitRepo
          value: https://github.com/adamancini/kargo-quickstart
      steps:
        - uses: git-clone
          config:
            repoURL: ${{ vars.gitRepo }}
            checkout:
              - branch: main
                path: ./repo
        - uses: yaml-update
          config:
            path: ./repo/akuity/akkoma-appset.yaml
            updates:
              - key: spec.generators.0.list.elements.2.chart_version
                value: ${{ chartFrom("oci://ghcr.io/adamancini/charts/akkoma").Version }}
        - uses: git-commit
          config:
            path: ./repo
            message: >-
              chore: promote akkoma ${{ chartFrom("oci://ghcr.io/adamancini/charts/akkoma").Version }} to prod
        - uses: git-push
          config:
            path: ./repo
            targetBranch: main
        - uses: argocd-update
          config:
            apps:
              - name: akkoma-${{ ctx.stage }}
                namespace: argocd
                sources:
                  - repoURL: "ghcr.io/adamancini/charts"
                    chart: akkoma
                    desiredRevision: ${{ chartFrom("oci://ghcr.io/adamancini/charts/akkoma").Version }}
